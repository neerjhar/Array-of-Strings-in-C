# Array-of-Strings-in-C
```
#include <stdio.h>

int main() {

    // Creating array of strings for 3 strings
    char arr[3][10] = {"Geek", "Geeks",
                       "Geekfor"};
  
    for (int i = 0; i < 3; i++)
        printf("%s\n", arr[i]);
    return 0;
}

```
